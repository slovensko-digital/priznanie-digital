import { exectueTestcase, executePostponeCase } from './executeCase'

describe('Cases', () => {
  ;[
    // 'withEmploymentBonus',
    // 'withHighIncome',
    // 'bugReport1',
    // 'bugReport2',
    'bugReport3',
  ].forEach(exectueTestcase)
})
